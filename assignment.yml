# create the list by passing the variables from extra vars 
# create the list by passing the variables from vars_files
# print content of the list
# merge the two lists
# split the list into two lists
# install 5 packages and store the result in a list after that print on the screen with package name and installation 
# status

# package name: [nginx, nginx1, tree, invalid]
# dictionary and map, string (study), filter